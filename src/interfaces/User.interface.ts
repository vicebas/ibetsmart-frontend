export type User = {
    id: string;
    name: string;
    email: string;
    profile_pic: string;
}

export type  LoginData = {
    email: string;
    password: string;
}

export type SignupData = {
    name: string;
    email: string;
    password: string;
}